CREATE TABLE bytejta_one (
  xid  bigint,
  gxid varchar(40),
  bxid varchar(40),
  ctime bigint(20),
  PRIMARY KEY (xid)
);

CREATE TABLE bytejta_two (
  xid  bigint,
  gxid varchar(40),
  bxid varchar(40),
  ctime bigint(20),
  PRIMARY KEY (xid)
);